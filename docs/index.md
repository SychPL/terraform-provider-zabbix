---
page_title: "zabbix Provider"
subcategory: ""
description: |-
  Manage Zabbix host groups, hosts, media types and actions through the Zabbix JSON-RPC API.
---

# zabbix Provider

Manages Zabbix objects through the JSON-RPC API. Tested against Zabbix 6.4.

Authenticate with an API token (`api_token`, recommended) or with `username` and
`password`. All arguments can also be provided through `ZABBIX_*` environment
variables. Sensitive values are masked in CLI output but stored in the Terraform
state.

## Example Usage

```terraform
terraform {
  required_providers {
    zabbix = {
      source = "Tensai123/zabbix"
    }
  }
}

# Authenticate with an API token (recommended) ...
provider "zabbix" {
  url       = "https://zabbix.example.com/api_jsonrpc.php"
  api_token = var.zabbix_api_token # or ZABBIX_API_TOKEN
}

# ... or with username and password.
# provider "zabbix" {
#   url      = "https://zabbix.example.com/api_jsonrpc.php"
#   username = "Admin"
#   password = var.zabbix_password
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `url` (String) Zabbix API JSON-RPC endpoint URL (e.g. http://localhost/zabbix/api_jsonrpc.php)

### Optional

- `api_token` (String, Sensitive) Zabbix API Token (Bearer)
- `ca_cert_file` (String) Path to CA certificate file for SSL verification
- `password` (String, Sensitive) Zabbix API password
- `tls_insecure` (Boolean) Disable SSL verification for HTTPS Zabbix endpoint
- `username` (String) Zabbix API username
